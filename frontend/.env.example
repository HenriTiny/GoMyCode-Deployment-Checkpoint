# Leave empty for local development with proxy (recommended)
# Or set to http://localhost:3001 if proxy not configured
# Or set to your Azure app URL for production: https://your-app.azurewebsites.net
REACT_APP_API_URL=
